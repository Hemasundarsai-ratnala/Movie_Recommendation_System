# Optional: Backend API Base URL
# If empty, Vite will proxy all /api requests to http://127.0.0.1:8000 automatically
# If deploying frontend to a separate host (e.g. Vercel, Netlify), set this to your Render backend URL:
# VITE_API_BASE_URL=https://movie-recommendation-system-i8o4.onrender.com
VITE_API_BASE_URL=
