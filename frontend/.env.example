# Optional: Backend API Base URL
# If empty, Vite will proxy all /api requests to http://127.0.0.1:8000 automatically
# If deploying frontend to a separate host (e.g. Netlify/Cloudflare), set this to your backend domain:
# VITE_API_BASE_URL=https://your-backend-domain.com
VITE_API_BASE_URL=
